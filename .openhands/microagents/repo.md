# OpenHands Rules — AI SOP v2 Workflow

> Auto-generated by [`ai-dev-toolkit`](.) — DO NOT EDIT MANUALLY unless you understand the implications.
> Template for SOP v2 risk-graded development workflow.

---

## 1. Core Principles

- **Plan first, then modify** — Always analyze, plan, and confirm the approach before writing any code.
- **Small tasks, no big rewrites** — Break work into small, focused changes. Do not undertake large refactors unless explicitly tasked.
- **Explicit allowed & forbidden scope** — Clearly define which files may be modified and which must not be touched. Strictly adhere to allowed files / forbidden files清单.
- **Default forbidden items** — Do not modify `.env`, secrets, tokens, API keys, production configuration, or real database connection configuration.
- **No automatic `git commit`** — Committing requires explicit human instruction.
- **No automatic `git push`** — Pushing requires explicit human approval.
- **No automatic `git merge main`** — Merging requires explicit human approval.
- **No large-scale refactoring** — Refactor only as instructed and scoped.
- **Do not expand task scope** — Never自行扩大任务范围 beyond what was requested.
- **Retry limit** — On failure, retry at most **2 times**, then stop and report to the human.
- **High-risk tasks require human confirmation** — Never auto-execute high-risk changes; wait for人工确认 before proceeding.

---

## 2. Risk Levels

| Level       | Description | Auto-Retry Limit | Human Review Required |
|-------------|-------------|------------------|-----------------------|
| `readonly`  | Read-only analysis, documentation review, code explanation | None (no changes) | No |
| `low`       | Bug fix, test update, minor doc change, small config tweak | Up to 2 auto retries | No |
| `medium`    | Scoped feature, moderate refactor, dependency update without security or schema impact | Up to 1 auto retry | No |
| `high`      | Authentication, permissions, DB schema, policy, audit, auto-exec commands, production config, data deletion, payment, privacy data | No auto retry | **Yes — must be confirmed by human before execution** |
| `milestone` | Major release, cross-cutting change, breaking API, security audit | No auto retry | **Yes — requires multi-tool review before execution** |

---

## 3. Startup Reading Order

When starting a task, read the following files **in order** (if they exist):

1. [`AGENTS.md`](./AGENTS.md) — Agent roles and responsibilities
2. [`.ai-dev-rules.md`](./.ai-dev-rules.md) — Project development rules and risk grading
3. [`PROJECT_STATUS.md`](./PROJECT_STATUS.md) — Current project state and progress
4. [`TOOL_DECISION.md`](./TOOL_DECISION.md) — Tool selection and decision log
5. [`REVIEW_POLICY.md`](./REVIEW_POLICY.md) — Code review policies
6. [`TASK_QUEUE.md`](./TASK_QUEUE.md) — Task queue and prioritization

---

## 4. Prohibited Operations

| # | Prohibited Action | Rationale |
|---|-------------------|-----------|
| 1 | Modifying `.env`, `.env.*`, or any file containing secrets | Security — these are human-managed |
| 2 | Modifying secrets, tokens, API keys, or real database connection config | Security — must never be exposed or altered by AI |
| 3 | Automatic `git commit` without explicit human instruction | Prevents unintended commits |
| 4 | Automatic `git push` without explicit human approval | Prevents unintended remote changes |
| 5 | Automatic `git merge main` without explicit human approval | Prevents unintended branch merges |
| 6 | Deleting files or directories (unless explicitly allowed by the task) | Irreversible data loss risk |
| 7 | Running destructive commands (`rm -rf`, `git reset --hard`, `git clean -fd`) | Irreversible data loss risk |
| 8 | Creating files outside the allowed modification scope | Scope creep and unintended side effects |
| 9 | Creating abnormally named files (natural language names, `SEARCH/REPLACE`, `Let's`, `Here is`) | Causes confusion and tooling issues |
| 10 | Saving `plans/` or implementation plan files (unless explicitly allowed) | Plans should be communicated, not persisted |
| 11 | Large-scale refactoring | Must be explicitly scoped and instructed |
| 12 | Expanding task scope beyond what was requested | Scope creep |

---

## 5. Post-Completion Output Requirements

After completing any task, you **must** output the following in your completion message:

1. **Modified files** — List all files that were changed.
2. **Summary of changes** — What was changed in each file.
3. **Tests executed** — Which tests were run.
4. **Test results** — Whether tests passed or failed.
5. **Risk points** — Any remaining risk concerns.
6. **Forbidden scope check** — Confirm whether any forbidden files or actions were touched.
7. **No `git commit` confirmation** — Explicitly state that no commit was automatically made.
8. **No `git push` confirmation** — Explicitly state that no push was automatically made.
9. **No `merge main` confirmation** — Explicitly state that no merge was automatically performed.

### Example Output

```
## Completion Report

1. **Modified files**: `src/auth.ts`, `src/user-service.ts`
2. **Changes**: Added input validation to login endpoint; fixed SQL injection in user query
3. **Tests executed**: `npm test` (unit + integration)
4. **Test results**: 42 passed, 0 failed
5. **Risk points**: Low — no auth/permissions changes beyond input validation
6. **Forbidden scope check**: No forbidden files or actions touched ✅
7. **No git commit**: ✅ Confirmed
8. **No git push**: ✅ Confirmed
9. **No merge main**: ✅ Confirmed
```

---

## 6. High-Risk Task Handling

Any change touching these areas is classified as **high** risk and **must stop and wait for human confirmation** before execution:

- **Authentication** — Login, signup, OAuth, session management, JWT, password handling
- **Authorization / Permissions** — RBAC, ACL, role/scope checks, access control
- **Database schema** — Migration, table creation, column changes, index changes, data type changes
- **Policy** — Security policy, compliance policy, terms of service
- **Audit** — Audit log changes, audit trail modifications
- **Auto-executed commands** — Any command that runs automatically with side effects
- **GitHub token / CI tokens** — Any credential used in CI/CD
- **Data deletion** — `DROP`, `DELETE`, `TRUNCATE`, file deletion, data purge
- **Production deployment** — Any change that affects production environments
- **Payment / Investment / Auto-trading** — Financial transaction logic
- **User privacy data** — PII, GDPR-related, user data export/import

> **Rule**: For any high-risk task:
> 1. Flag the risk level upfront.
> 2. Present the plan to the human.
> 3. Wait for explicit confirmation before making any changes.
> 4. After implementation, request human review before commit.

---

## 7. Quality Gate

All changes **must** pass the quality gate before completion:

- The post-hook [`hooks/quality_gate.sh`](../hooks/quality_gate.sh) is automatically invoked after task execution.
- If the quality gate exits with a non-zero status, review the reported issues and fix them.
- Do not bypass the quality gate under any circumstances.

---

*Generated by [`ai-dev-toolkit`](.) — SOP v2 Risk-Graded OpenHands Rules Template*
