# Task Slip — Next Zoo Task

> Auto-generated by [`ai-dev-toolkit`](..) — AI SOP v2 template.
> Copy this file to your target project's `.ai/tasks/` and fill in the fields.
> Intended for use with **Zoo Code** (or compatible AI coding agents).

---

```yaml
---
Task ID:              <T-NNN>
Title:                <Short, descriptive title>
Risk Level:           <readonly | low | medium | high | milestone>
Recommended Tool:     Zoo Code
---
```

## Background

<!-- Why does this task exist? What is the context? -->

## Goal

<!-- What must be achieved? One clear sentence. -->

---

## Scope

### Allowed Files

<!-- Glob patterns or explicit paths the AI may modify. Example:
  - src/components/Button.tsx
  - src/styles/button.css
-->

### Forbidden Files

<!-- Glob patterns or explicit paths the AI must NOT touch. Example:
  - src/auth/
  - .env
  - databases/
-->

---

## Requirements

<!--
Specific constraints and rules for this task.
Be explicit — ambiguity leads to mistakes.
-->

---

## Acceptance Criteria

<!--
Measurable, verifiable conditions. When all are met, the task is done.
Example:
  - [ ] Input validation checks for empty fields
  - [ ] Error message is displayed below the form
  - [ ] All existing tests still pass
-->

---

## Test Commands

<!--
Commands to verify correctness. Example:
  - npm run lint
  - npm test
  - npm run build
-->

---

## Review Requirements

<!--
Who or what must review the output before it is considered final.
Example:
  - [ ] Auto-review only (low-risk task)
  - [ ] Peer review required (medium/high-risk task)
  - [ ] Security review required (high/milestone task)
-->

---

## Stop Conditions

The AI **must stop and ask for human guidance** if any of the following occur:

- [ ] A file outside the allowed scope needs to be modified
- [ ] A new file not listed in the allowed scope needs to be created
- [ ] The task involves `.env`, secrets, tokens, or API keys
- [ ] The task involves database schema changes, permissions, authentication,
      authorization policies, audit logic, data deletion, or production deployment
      (if triggered, **upgrade risk to `high`** and wait for human confirmation)
- [ ] The AI is about to `git commit`
- [ ] The AI is about to `git push`
- [ ] The AI is about to `git merge main`

> **When stopped:** Provide a clear summary of what triggered the stop and
> what action the human should take.

---

## Completion Report

After finishing the task, the AI **must output** the following completion report:

1. **Files modified** — List every file that was changed
2. **What changed in each file** — Summarize the change per file
3. **Tests run** — Which test commands were executed
4. **Tests passed?** — Yes / No (if no, explain)
5. **Risk points** — Any remaining risks or concerns
6. **Human review needed?** — Yes / No
7. **`git commit` confirmed?** — No (must not commit)
8. **`git push` confirmed?** — No (must not push)

---

*Template provided by [`ai-dev-toolkit`](..)*
