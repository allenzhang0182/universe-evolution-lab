# AGENTS.md — AI Tool Unified Entry Point

> Auto-generated by [`ai-dev-toolkit`](.) — AI SOP v2 template.
> Copy this file to your project root as the unified entry point for all AI tools.

---

## 1. Read Order

When an AI tool starts working on this project, it **must read the following files in order** before making any modifications:

1. **`AGENTS.md`** (this file) — Tool entry point and agent coordination
2. **`.ai-dev-rules.md`** — Core development rules and coding standards
3. **`PROJECT_STATUS.md`** — Current project state, progress, and known issues
4. **`TOOL_DECISION.md`** — Architecture decisions and tool selection rationale
5. **`REVIEW_POLICY.md`** — Risk classification and gating policy
6. **`TASK_QUEUE.md`** — Task queue with status and assignments

---

## 2. Default Development Principles

All AI tools must adhere to the following principles:

- **Plan first, then execute** — Always read the relevant context files before writing code.
- **Small tasks, no big bangs** — Prefer small, incremental changes over massive refactors.
- **Explicit allowed scope** — Only modify files explicitly listed in the allowed scope.
- **Explicit forbidden scope** — Never touch files listed as forbidden.
- **Tests must pass before submission** — Run the test suite after every change.
- **High-risk tasks require human review** — See [`REVIEW_POLICY.md`](REVIEW_POLICY.md) for risk classification.
- **Default: do not `git push`** — No automatic pushes to remote.
- **Default: do not `git merge main`** — No automatic merges from main branch.

---

## 3. Tool Division of Labor

| Tool | Role | When to Use |
|------|------|-------------|
| **ChatGPT** | Planning, task breakdown, prompt generation, review | Architecture analysis, generating subtask prompts, PR/code review |
| **Zoo Code + DeepSeek** | Default primary driver, multi-file tasks | Most development work, especially tasks spanning multiple files |
| **Aider + DeepSeek** | Scoped low-risk small tasks | Clearly defined file scope, low/medium risk, small changes |
| **OpenHands** | Sandbox execution for clear issues | Low/medium GitHub issues requiring isolated execution environment *(future)* |
| **SWE-agent** | Automated GitHub issue fix experiments | Clear GitHub issues with reproducible steps *(future)* |
| **CodeRabbit** | PR auto-review | Automated review of every pull request |
| **GitHub Actions** | CI gating (tests, lint, build) | Automated quality gates on every PR/push |
| **Codex / Claude / Cursor** | High-risk and milestone reviews | Security audits, schema changes, production deployments, milestone sign-off |

---

## 4. Prohibited Operations

AI tools **must not** perform the following under any circumstances:

- Modify `.env`, `.env.*`, or any file containing secrets, tokens, or API keys
- Modify production configuration files
- `git push` to any branch without explicit human approval
- `git merge main` or any branch merge without explicit human approval
- Large-scale refactoring not explicitly scoped in the task
- Expand the task scope beyond what is defined in the task description
- Create new files outside the explicitly allowed file path list
- Run destructive commands (`rm -rf`, `git reset --hard`, `git clean -fd`) without confirmation

---

## 5. Project Boundaries

This project — **`ai-dev-toolkit`** — is limited to:

- Static rule templates (`.ai-dev-rules.md`, `.clinerules`, `.roorules`, etc.)
- Project initialization templates
- SOP v2 import scripts
- SOP v2 validation scripts
- Agent coordination templates (`AGENTS.md`, `REVIEW_POLICY.md`, `TASK_QUEUE.md`)
- GitHub Issue / PR / CI / CodeRabbit template distribution

**`ai-dev-toolkit` does NOT cover:**

- Dynamic task management
- Risk-level execution engine
- Approval workflow system
- Audit logging system
- Automated development loop controller
- Web UI
- Agent platform

These capabilities belong to **`ai-project-brain`**, a separate system.

---

## 6. Aider Usage Reminders

> Quick-entry checklist before using Aider or any AI execution tool.

### 6.1 Pre-Flight Checklist

- [ ] **Check allowed files** — Confirm the task's `Allowed files` / `Allowed directories` list is defined and understood.
- [ ] **Never add all files automatically** — Reject `all files`, `auto add`, and `don't ask again` prompts from Aider.
- [ ] **Reject suspicious file creation** — If Aider proposes a file with an abnormal name (natural language, `SEARCH/REPLACE`, `Let's`, `Here is`, patch snippets), reject it and stop the task.
- [ ] **Stop if scope expands** — If the tool suggests modifying files outside the allowed scope, stop and escalate to a human.
- [ ] **No auto-commit** — Do not let Aider `git commit` automatically; require human approval.
- [ ] **No auto-push** — Do not let Aider `git push` under any circumstances.

### 6.2 When to Avoid Aider

- **High-risk tasks** — Never delegate authentication, permissions, database schema, production config, or audit tasks to Aider.
- **Unclear file scope** — If the task does not list explicit file paths, use Zoo Code + DeepSeek or ChatGPT for planning first.
- **Multi-file changes** — Aider struggles with 3+ files; use Zoo Code + DeepSeek instead.

---

*Generated by [`ai-dev-toolkit`](.) — SOP v2 template.*
